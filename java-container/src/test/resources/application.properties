hams.address=0.0.0.0:8081
hams.prefix=hams
hams.checks.timeout=100
hams.checks.fails=1
hams.checks.preflights[0]=test
hams.checks.shutdowns[0]=test

runtime.threads=1
runtime.stack-size=1024
runtime.name=test-runtime

webservice.address=http://0.0.0.0:8080
webservice.prefix=/api/v1/dynamic

kafka.brokers=kafka://localhost:9092
kafka.group-id=test-group
kafka.topic=test-topic
kafka.schema-registry-url=http://localhost:8081
kafka.cache-max-age=10s
kafka.fetch-metadata-timeout=1s
kafka.offset-reset=EARLIEST
kafka.force-reset-earliest=false

startup-checks.fails=1
startup-checks.timeout=100ms
startup-checks.enabled=false
